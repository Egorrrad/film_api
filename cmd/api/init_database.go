package main

/*
CREATE TABLE actors
(
    id SERIAL PRIMARY KEY,
    name  varchar(50),
    gender  varchar(15),
    birhday INTEGER,
    films varchar(150)[]
);

CREATE TABLE films
(
    id SERIAL PRIMARY KEY,
    name  varchar(150),
    description  varchar(1000),
    date varchar(8),
    rating INTEGER,
    Actors varchar(50)[]
);
*/
